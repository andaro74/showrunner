"""Shared pytest fixtures.

Will hold: httpx mock-transport fixtures and sample upstream payloads
(TVmaze show, Overpass amenities, OSRM route). Populated alongside the
client tests in build-order steps 2, 4.
"""

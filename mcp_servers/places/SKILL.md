# places MCP server

Keyless location data over OpenStreetMap (Nominatim/Overpass/OSRM).

## Tools

<!-- One line per tool. Add an entry here whenever a tool is added (CLAUDE.md hard rule #3). -->

- `geocode` — _(step 5)_
- `find_nearby` — _(step 5)_
- `travel_time` — _(step 5)_

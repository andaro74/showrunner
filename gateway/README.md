# gateway/

AgentCore **Gateway** config — managed tool routing/auth; a production alternative
to self-hosting the MCP servers.

**Ordering rule (CLAUDE.md hard rule #2):** if Gateway is enabled, configure
**Identity first** — Gateway relies on Identity's OAuth provider.

Config lands in build-order step 9 (PROJECT.md Phase 11). Placeholder for now.
